name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            name: macOS-arm64
            arch: aarch64
          - platform: macos-latest
            target: x86_64-apple-darwin
            name: macOS-x64
            arch: x64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            name: Windows-x64
            arch: x64

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install create-dmg

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          args: --target ${{ matrix.target }}

      - name: Rename macOS updater artifacts with arch
        if: runner.os == 'macOS'
        run: |
          BUNDLE_DIR="src-tauri/target/${{ matrix.target }}/release/bundle/macos"
          if [ -f "$BUNDLE_DIR/D-Mate.app.tar.gz" ]; then
            mv "$BUNDLE_DIR/D-Mate.app.tar.gz" "$BUNDLE_DIR/D-Mate_${{ matrix.arch }}.app.tar.gz"
          fi
          if [ -f "$BUNDLE_DIR/D-Mate.app.tar.gz.sig" ]; then
            mv "$BUNDLE_DIR/D-Mate.app.tar.gz.sig" "$BUNDLE_DIR/D-Mate_${{ matrix.arch }}.app.tar.gz.sig"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: D-Mate-${{ matrix.name }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe.sig
            src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz
            src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz.sig
          if-no-files-found: ignore

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Generate latest.json for Tauri updater
        run: |
          VERSION="${GITHUB_REF_NAME#v}"

          MAC_ARM_SIG=$(find artifacts -name "D-Mate_aarch64.app.tar.gz.sig" -exec cat {} \; 2>/dev/null || echo "")
          MAC_X64_SIG=$(find artifacts -name "D-Mate_x64.app.tar.gz.sig" -exec cat {} \; 2>/dev/null || echo "")
          WIN_SIG=$(find artifacts -name "*x64-setup.exe.sig" -exec cat {} \; 2>/dev/null || echo "")

          echo "MAC_ARM_SIG length: ${#MAC_ARM_SIG}"
          echo "MAC_X64_SIG length: ${#MAC_X64_SIG}"
          echo "WIN_SIG length: ${#WIN_SIG}"

          cat > artifacts/latest.json << ENDJSON
          {
            "version": "${VERSION}",
            "notes": "D-Mate ${GITHUB_REF_NAME} 업데이트",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "darwin-aarch64": {
                "url": "https://github.com/dice-book-p/d-mate/releases/download/${GITHUB_REF_NAME}/D-Mate_aarch64.app.tar.gz",
                "signature": "${MAC_ARM_SIG}"
              },
              "darwin-x86_64": {
                "url": "https://github.com/dice-book-p/d-mate/releases/download/${GITHUB_REF_NAME}/D-Mate_x64.app.tar.gz",
                "signature": "${MAC_X64_SIG}"
              },
              "windows-x86_64": {
                "url": "https://github.com/dice-book-p/d-mate/releases/download/${GITHUB_REF_NAME}/D-Mate_${VERSION}_x64-setup.exe",
                "signature": "${WIN_SIG}"
              }
            }
          }
          ENDJSON

          echo "=== latest.json ==="
          cat artifacts/latest.json

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "D-Mate ${{ github.ref_name }}"
          draft: false
          body: |
            ## D-Mate ${{ github.ref_name }}

            ### 다운로드
            | OS | 파일 |
            |------|------|
            | macOS (Apple Silicon) | `D-Mate_*_aarch64.dmg` |
            | macOS (Intel) | `D-Mate_*_x64.dmg` |
            | Windows | `D-Mate_*_x64-setup.exe` |

            ---

            ### macOS 설치
            1. DMG 다운로드 → 열기 → **Applications 폴더로 드래그**
            2. 터미널에서 보안 해제 (최초 1회):
            ```bash
            xattr -cr /Applications/D-Mate.app
            ```
            3. Applications에서 D-Mate 실행

            ### Windows 설치
            1. `.exe` 다운로드 → 브라우저 경고 시 **"유지"** 클릭
            2. **스마트 앱 컨트롤** 차단 시: 설정 → Windows 보안 → 앱 및 브라우저 컨트롤 → 스마트 앱 컨트롤 → **끄기**
            3. SmartScreen 경고 시: **"추가 정보" → "실행"**
          files: artifacts/**/*
